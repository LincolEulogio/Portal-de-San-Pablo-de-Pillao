---
import suppliers from '../data/suppliers.json';
---

<section id="proveedores" class="py-16 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-primary mb-4">Proveedores de Insumos</h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Encuentra todo lo necesario para tu producción agrícola y pecuaria.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      {suppliers.map((supplier) => (
        <div class="flex flex-col md:flex-row bg-white rounded-xl shadow-lg overflow-hidden border border-gray-200">
          <div class="md:w-1/3 bg-gray-100 flex items-center justify-center p-4">
            <span class="text-6xl">🚜</span>
            <!-- <img src={supplier.image} alt={supplier.name} class="w-full h-full object-cover" /> -->
          </div>
          
          <div class="p-6 md:w-2/3 flex flex-col justify-between">
            <div>
              <h3 class="text-xl font-bold text-gray-800 mb-2">{supplier.name}</h3>
              <p class="text-sm text-gray-500 mb-4 flex items-center">
                <span class="mr-1">📍</span> {supplier.address}
              </p>
              
              <div class="mb-4">
                <h4 class="text-sm font-bold text-gray-700 uppercase mb-2">Productos:</h4>
                <div class="flex flex-wrap gap-2">
                  {supplier.supplies.map(supply => (
                    <span class="px-2 py-1 bg-gray-100 text-gray-700 text-xs rounded border border-gray-200">
                      {supply}
                    </span>
                  ))}
                </div>
              </div>
            </div>
            
            <div class="flex items-center justify-between mt-4 pt-4 border-t border-gray-100">
              <div class="text-sm">
                <span class="block text-gray-500 text-xs">Horario:</span>
                <span class="font-medium text-gray-800">{supplier.hours}</span>
              </div>
              <a 
                href={`https://wa.me/${supplier.phone.replace(/\D/g, '')}`} 
                class="px-4 py-2 bg-accent text-white rounded-lg text-sm font-bold hover:bg-accent-dark transition-colors"
              >
                Consultar
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
