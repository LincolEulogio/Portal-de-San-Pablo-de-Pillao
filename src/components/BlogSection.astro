---
const posts = [
  {
    title: "EL SENDERO DEL GALLITO DE LAS ROCAS",
    excerpt: "Descubre la biodiversidad en el ACR Codo del Pozuzo",
    image: "/images/tour-nature.jpg",
  },
  {
    title: "Los Negritos de Huánuco",
    excerpt: "La Navidad más larga del Perú",
    image: "/images/blog-negritos.jpg",
  },
  {
    title: "Salud y sabor en la gastronomía Huanuqueña",
    excerpt: "Descubre los sabores únicos de nuestra región",
    image: "/images/tour-nature.jpg",
  },
];
---

<section class="section blog-section" id="blog">
  <div class="container">
    <div class="section-header">
      <h2>Blog</h2>
      <p>
        Explora la magia de Huánuco a través de nuestra mirada local. Historias,
        consejos y secretos de esta joya peruana te esperan.
      </p>
    </div>

    <div class="blog-grid">
      {
        posts.map((post) => (
          <article class="blog-card card">
            <div class="blog-image">
              <img src={post.image} alt={post.title} />
            </div>
            <div class="blog-content">
              <h3>{post.title}</h3>
              <p>{post.excerpt}</p>
              <a href="#" class="read-more">
                Leer más →
              </a>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .blog-section {
    background-color: var(--color-bg-light);
  }

  .blog-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: var(--space-8);
  }

  .blog-card {
    overflow: hidden;
  }

  .blog-image {
    width: 100%;
    height: 250px;
    overflow: hidden;
  }

  .blog-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .blog-card:hover .blog-image img {
    transform: scale(1.05);
  }

  .blog-content {
    padding: var(--space-6);
  }

  .blog-content h3 {
    color: var(--color-text-dark);
    font-size: var(--text-xl);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .blog-content p {
    color: var(--color-text-light);
    margin-bottom: var(--space-4);
  }

  .read-more {
    color: var(--color-primary);
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    transition: gap var(--transition-fast);
  }

  .read-more:hover {
    gap: var(--space-3);
  }
</style>
